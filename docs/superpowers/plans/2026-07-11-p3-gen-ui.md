# P3 — Gen UI（AI 动态卡片）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把现有"后端硬编码假 tool（provideLinks）"升级为真正的 AI 驱动 gen UI：后端用 AI SDK `streamText` + `tool()` 接 DashScope/qwen（function calling），模型自主决定调用哪个卡片 tool、填什么数据；前端按 tool part 的 type 渲染富卡片（复用 P1 组件）。

**Architecture:** 新增 `/api/genui/chat` 路由用 `streamText` + `tools`（provideProductComparison / providePricing / provideSolution），`toUIMessageStreamResponse()` 输出 SSE。工具的 `execute` 从 P2 的数据仓取真实数据。前端新增一个营销站 gen UI 面板组件，在 message parts 里按 `tool-<name>` 映射到卡片组件。保留现有 `/api/chat`（Dify RAG）不动，gen UI 是并行新通道。

**Tech Stack:** ai@6.0.101（`streamText`, `tool`, `stepCountIs`, `convertToModelMessages`, `toUIMessageStreamResponse`）, @ai-sdk/openai-compatible@2.0.30（接 DashScope compatible-mode）, @ai-sdk/react@3.0.103（`useChat`）, zod@4, P1 marketing 组件, P2 products-repo。

## Global Constraints

- 前置：P0（可独立运行）必须完成；P1（卡片组件）与 P2（数据仓）强依赖——卡片渲染目标来自 P1，工具数据来自 P2。执行顺序 P0→P1→P2→P3。
- 模型接入：DashScope OpenAI 兼容模式（`https://dashscope.aliyuncs.com/compatible-mode/v1`），模型 `qwen-plus`（memory 记录：默认 qwen3.6-plus 免费额度可能 403，用 qwen-plus/qwen-max 稳定）。凭据用新增环境变量，不复用平台加密存储（独立项目）。
- 不破坏现有 Dify 通道：`/api/chat` 与 `components/ai/search.tsx` 保持可用；gen UI 用新路由 `/api/genui/chat` 与新组件。
- tool 输出 schema = 卡片 props 契约，用 zod 定义，前后端共享同一 schema 文件。
- SSE：沿用 AI SDK UI Message Stream（`toUIMessageStreamResponse`），不手工构流。
- 环境变量新增：`DASHSCOPE_API_KEY`、`DASHSCOPE_BASE_URL`、`GENUI_MODEL`。进 `.env.example`。
- TDD：tool schema 校验、卡片 part 选择逻辑用单测；端到端流用集成验证。

---

### Task 1: gen UI 工具 schema（前后端共享契约）

**Files:**
- Create: `/Users/mac_1/msru.web/lib/ai/genui-schema.ts`
- Test: `/Users/mac_1/msru.web/lib/ai/genui-schema.test.ts`

**Interfaces:**
- Produces（zod schema + 推断类型，被后端 tool 定义与前端渲染共用）：
  - `ProductComparisonSchema` → `{ products: { slug, title, tagline, accentColor }[] }`
  - `PricingSchema` → `{ plans: { name, price, unit, features: string[], href }[] }`
  - `SolutionSchema` → `{ title, description, bullets: string[], ctaHref }`
  - 对应类型：`ProductComparisonInput`、`PricingInput`、`SolutionInput`
  - `GENUI_TOOL_NAMES = ["provideProductComparison","providePricing","provideSolution"] as const`

- [ ] **Step 1: 写失败测试**

写 `/Users/mac_1/msru.web/lib/ai/genui-schema.test.ts`：
```ts
import { expect, test } from "vitest";
import { PricingSchema, ProductComparisonSchema, SolutionSchema } from "./genui-schema";

test("ProductComparisonSchema accepts valid input", () => {
  const r = ProductComparisonSchema.safeParse({
    products: [{ slug: "mes", title: "MES", tagline: "生产", accentColor: "blue" }],
  });
  expect(r.success).toBe(true);
});

test("PricingSchema requires features array", () => {
  const r = PricingSchema.safeParse({
    plans: [{ name: "Pro", price: "￥999", unit: "/月", features: ["A", "B"], href: "/pricing" }],
  });
  expect(r.success).toBe(true);
});

test("SolutionSchema rejects missing title", () => {
  const r = SolutionSchema.safeParse({ description: "d", bullets: [], ctaHref: "/x" });
  expect(r.success).toBe(false);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-schema 2>&1 | tail -8`
Expected: FAIL。

- [ ] **Step 3: 实现 genui-schema.ts**

写 `/Users/mac_1/msru.web/lib/ai/genui-schema.ts`：
```ts
import { z } from "zod";

export const ProductComparisonSchema = z.object({
  products: z
    .array(
      z.object({
        slug: z.string(),
        title: z.string(),
        tagline: z.string(),
        accentColor: z.string(),
      }),
    )
    .min(1),
});

export const PricingSchema = z.object({
  plans: z
    .array(
      z.object({
        name: z.string(),
        price: z.string(),
        unit: z.string(),
        features: z.array(z.string()),
        href: z.string(),
      }),
    )
    .min(1),
});

export const SolutionSchema = z.object({
  title: z.string(),
  description: z.string(),
  bullets: z.array(z.string()),
  ctaHref: z.string(),
});

export type ProductComparisonInput = z.infer<typeof ProductComparisonSchema>;
export type PricingInput = z.infer<typeof PricingSchema>;
export type SolutionInput = z.infer<typeof SolutionSchema>;

export const GENUI_TOOL_NAMES = [
  "provideProductComparison",
  "providePricing",
  "provideSolution",
] as const;
export type GenUiToolName = (typeof GENUI_TOOL_NAMES)[number];
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-schema 2>&1 | tail -8`
Expected: PASS（3 tests）。

- [ ] **Step 5: Commit**

```bash
cd /Users/mac_1/msru.web && git add lib/ai/genui-schema.ts lib/ai/genui-schema.test.ts && \
git commit -m "feat(p3): shared zod schema for gen UI tools (front/back contract)"
```

---

### Task 2: 模型 provider + gen UI tools 定义

**Files:**
- Create: `/Users/mac_1/msru.web/lib/ai/provider.ts`
- Create: `/Users/mac_1/msru.web/lib/ai/genui-tools.ts`
- Test: `/Users/mac_1/msru.web/lib/ai/genui-tools.test.ts`

**Interfaces:**
- Consumes: `@ai-sdk/openai-compatible` 的 `createOpenAICompatible`；P2 的 `getProducts`；Task 1 schema。
- Produces:
  - `function genuiModel()` — 返回配置好的 DashScope 兼容模型。
  - `const genuiTools` — `{ provideProductComparison, providePricing, provideSolution }`，每个用 `tool({ description, inputSchema, execute })`。`execute` 从 P2 数据仓取真实数据后返回（供 UI 渲染，同时回给模型）。

**说明（给实现者）：** tool 的 `inputSchema` 用 Task 1 schema。`execute` 里对 `provideProductComparison` 调 `getProducts()` 过滤出模型点名的 slug，保证卡片数据是真库数据而非模型编造。这是"gen UI 但数据可信"的关键。

- [ ] **Step 1: 写失败测试（tools 结构与 provideProductComparison.execute 用真数据）**

写 `/Users/mac_1/msru.web/lib/ai/genui-tools.test.ts`：
```ts
import { expect, test, vi } from "vitest";

vi.mock("@/lib/content/products-repo", () => ({
  getProducts: vi.fn(async () => [
    { slug: "mes", title: "MES 执行", tagline: "生产的艺术", description: "d", accentColor: "blue", icon: "Cpu" },
    { slug: "wms", title: "WMS 仓储", tagline: "极智流转", description: "d", accentColor: "emerald", icon: "Package" },
  ]),
}));

test("exposes three gen UI tools", async () => {
  const { genuiTools } = await import("./genui-tools");
  expect(Object.keys(genuiTools).sort()).toEqual(["providePricing", "provideProductComparison", "provideSolution"]);
});

test("provideProductComparison.execute pulls real data by slug", async () => {
  const { genuiTools } = await import("./genui-tools");
  const out = await genuiTools.provideProductComparison.execute({ slugs: ["mes", "wms"] }, { toolCallId: "t", messages: [] });
  expect(out.products).toHaveLength(2);
  expect(out.products[0]).toMatchObject({ slug: "mes", title: "MES 执行", accentColor: "blue" });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-tools 2>&1 | tail -8`
Expected: FAIL。

- [ ] **Step 3: 实现 provider.ts**

写 `/Users/mac_1/msru.web/lib/ai/provider.ts`：
```ts
import { createOpenAICompatible } from "@ai-sdk/openai-compatible";

export function genuiModel() {
  const provider = createOpenAICompatible({
    name: "dashscope",
    apiKey: process.env.DASHSCOPE_API_KEY ?? "",
    baseURL: process.env.DASHSCOPE_BASE_URL ?? "https://dashscope.aliyuncs.com/compatible-mode/v1",
  });
  return provider(process.env.GENUI_MODEL ?? "qwen-plus");
}
```

- [ ] **Step 4: 实现 genui-tools.ts**

写 `/Users/mac_1/msru.web/lib/ai/genui-tools.ts`：
```ts
import { tool } from "ai";
import { z } from "zod";
import { getProducts } from "@/lib/content/products-repo";
import { PricingSchema, SolutionSchema } from "./genui-schema";

export const genuiTools = {
  provideProductComparison: tool({
    description: "当用户想对比或了解多个产品时调用。传入产品 slug 列表（如 mes/wms/aps/qms/eam/iot/ai）。",
    inputSchema: z.object({ slugs: z.array(z.string()).min(1) }),
    async execute({ slugs }) {
      const all = await getProducts();
      const products = all
        .filter((p) => slugs.includes(p.slug))
        .map((p) => ({ slug: p.slug, title: p.title, tagline: p.tagline, accentColor: p.accentColor }));
      return { products };
    },
  }),
  providePricing: tool({
    description: "当用户询问价格、报价、套餐时调用，返回定价方案卡片。",
    inputSchema: PricingSchema,
    async execute(input) {
      return input;
    },
  }),
  provideSolution: tool({
    description: "当用户描述某个行业场景或需求、需要方案推荐时调用。",
    inputSchema: SolutionSchema,
    async execute(input) {
      return input;
    },
  }),
};
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-tools 2>&1 | tail -8`
Expected: PASS（2 tests）。

- [ ] **Step 6: Commit**

```bash
cd /Users/mac_1/msru.web && git add lib/ai/provider.ts lib/ai/genui-tools.ts lib/ai/genui-tools.test.ts && \
git commit -m "feat(p3): dashscope provider + gen UI tools with real-data execute"
```

---

### Task 3: gen UI 后端路由（streamText + tools + SSE）

**Files:**
- Create: `/Users/mac_1/msru.web/app/api/genui/chat/route.ts`

**Interfaces:**
- Consumes: `genuiModel`（Task 2）、`genuiTools`（Task 2）。
- Produces: POST `/api/genui/chat` — 接收 `{ messages }`，`streamText` 带 tools + system prompt，`toUIMessageStreamResponse()` 输出 SSE UI Message Stream。

- [ ] **Step 1: 写路由**

写 `/Users/mac_1/msru.web/app/api/genui/chat/route.ts`：
```ts
import { convertToModelMessages, stepCountIs, streamText, type UIMessage } from "ai";
import { genuiModel } from "@/lib/ai/provider";
import { genuiTools } from "@/lib/ai/genui-tools";

export const maxDuration = 30;

const SYSTEM_PROMPT = `你是 MSRU 工业软件官网的智能助手。回答用户问题时：
- 涉及多个产品对比或介绍时，调用 provideProductComparison（传产品 slug）。
- 涉及价格/套餐时，调用 providePricing。
- 涉及行业方案推荐时，调用 provideSolution。
- 其余情况正常用文字回答。调用工具后用一句话总结。`;

export async function POST(req: Request) {
  const { messages }: { messages: UIMessage[] } = await req.json();

  const result = streamText({
    model: genuiModel(),
    system: SYSTEM_PROMPT,
    messages: convertToModelMessages(messages),
    tools: genuiTools,
    stopWhen: stepCountIs(3),
  });

  return result.toUIMessageStreamResponse();
}
```

- [ ] **Step 2: 类型检查通过**

Run: `cd /Users/mac_1/msru.web && pnpm check-types 2>&1 | tail -15`
Expected: 无与本路由相关的类型错误（`streamText`/`toUIMessageStreamResponse`/`convertToModelMessages` API 匹配 ai@6）。

- [ ] **Step 3: 集成验证（起服 + 真实提问，需 DASHSCOPE_API_KEY）**

Run（`.env` 配好 `DASHSCOPE_API_KEY`）：
```bash
cd /Users/mac_1/msru.web && (pnpm dev &) && sleep 12 && \
curl -sS -N -X POST http://localhost:3007/api/genui/chat \
  -H 'content-type: application/json' \
  -d '{"messages":[{"role":"user","parts":[{"type":"text","text":"对比一下 MES 和 WMS"}]}]}' | head -c 600
echo; echo "--- expect SSE data: lines including tool-... parts ---"
```
Expected: 输出 SSE `data:` 行，含 text-delta 与 `tool-provideProductComparison` 相关 part（`tool-input`/`tool-output`）。停 dev。若 403 FreeTier，改 `GENUI_MODEL=qwen-max` 重试。

- [ ] **Step 4: Commit**

```bash
cd /Users/mac_1/msru.web && git add app/api/genui/chat && \
git commit -m "feat(p3): gen UI backend route (streamText + tools + SSE)"
```

---

### Task 4: 卡片渲染组件（按 tool part type 映射到 P1 组件）

**Files:**
- Create: `/Users/mac_1/msru.web/components/ai/genui-cards.tsx`
- Test: `/Users/mac_1/msru.web/components/ai/genui-cards.test.tsx`

**Interfaces:**
- Consumes: P1 的 `BentoCard`/`BentoGrid`（复用）；Task 1 schema 类型；lucide 图标映射。
- Produces: `function GenUiToolPart({ type, data }: { type: string; data: unknown }): JSX.Element | null` — 按 `tool-provideProductComparison` / `tool-providePricing` / `tool-provideSolution` 渲染对应卡片，未知 type 返回 null。

**说明（给实现者）：** 复用 P1 `BentoCard` 渲染产品对比卡。产品数据里 `icon` 是字符串名（如 "Cpu"），需一个 icon 名→组件映射；未匹配用默认图标。

- [ ] **Step 1: 写失败测试**

写 `/Users/mac_1/msru.web/components/ai/genui-cards.test.tsx`：
```tsx
import { render, screen } from "@testing-library/react";
import { expect, test } from "vitest";
import { GenUiToolPart } from "./genui-cards";

test("renders product comparison cards", () => {
  render(
    <GenUiToolPart
      type="tool-provideProductComparison"
      data={{ products: [{ slug: "mes", title: "MES 执行", tagline: "生产的艺术", accentColor: "blue" }] }}
    />,
  );
  expect(screen.getByText("MES 执行")).toBeInTheDocument();
});

test("renders pricing cards", () => {
  render(
    <GenUiToolPart
      type="tool-providePricing"
      data={{ plans: [{ name: "Pro", price: "￥999", unit: "/月", features: ["A"], href: "/pricing" }] }}
    />,
  );
  expect(screen.getByText("Pro")).toBeInTheDocument();
  expect(screen.getByText("A")).toBeInTheDocument();
});

test("returns null for unknown tool type", () => {
  const { container } = render(<GenUiToolPart type="tool-unknown" data={{}} />);
  expect(container).toBeEmptyDOMElement();
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-cards 2>&1 | tail -8`
Expected: FAIL。

- [ ] **Step 3: 实现 genui-cards.tsx**

写 `/Users/mac_1/msru.web/components/ai/genui-cards.tsx`：
```tsx
import { Boxes, Cpu, type LucideIcon, Network, Package, Settings, ShieldCheck, Zap } from "lucide-react";
import { BentoCard, BentoGrid } from "@/components/marketing";
import type { AccentColor } from "@/components/marketing";
import type { PricingInput, ProductComparisonInput, SolutionInput } from "@/lib/ai/genui-schema";

const ICONS: Record<string, LucideIcon> = { Cpu, Package, Settings, ShieldCheck, Network, Zap };
function iconFor(slug: string): LucideIcon {
  const map: Record<string, LucideIcon> = { mes: Cpu, wms: Package, eam: Settings, qms: ShieldCheck, iot: Network, ai: Zap };
  return map[slug] ?? Boxes;
}

export function GenUiToolPart({ type, data }: { type: string; data: unknown }) {
  if (type === "tool-provideProductComparison") {
    const { products } = data as ProductComparisonInput;
    return (
      <BentoGrid>
        {products.map((p) => (
          <BentoCard
            key={p.slug}
            icon={iconFor(p.slug)}
            accentColor={p.accentColor as AccentColor}
            title={p.title}
            tagline={p.tagline}
            description=""
          />
        ))}
      </BentoGrid>
    );
  }

  if (type === "tool-providePricing") {
    const { plans } = data as PricingInput;
    return (
      <div className="grid gap-4 sm:grid-cols-2">
        {plans.map((plan) => (
          <div key={plan.name} className="rounded-2xl border border-border/50 p-6">
            <h4 className="text-lg font-bold">{plan.name}</h4>
            <p className="mt-1 text-3xl font-bold">
              {plan.price}
              <span className="text-base text-muted-foreground">{plan.unit}</span>
            </p>
            <ul className="mt-4 space-y-1 text-sm text-muted-foreground">
              {plan.features.map((f) => (
                <li key={f}>{f}</li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    );
  }

  if (type === "tool-provideSolution") {
    const s = data as SolutionInput;
    return (
      <div className="rounded-2xl border border-border/50 p-6">
        <h4 className="text-xl font-bold">{s.title}</h4>
        <p className="mt-2 text-muted-foreground">{s.description}</p>
        <ul className="mt-4 space-y-1 text-sm">
          {s.bullets.map((b) => (
            <li key={b}>• {b}</li>
          ))}
        </ul>
      </div>
    );
  }

  return null;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-cards 2>&1 | tail -8`
Expected: PASS（3 tests）。

- [ ] **Step 5: Commit**

```bash
cd /Users/mac_1/msru.web && git add components/ai/genui-cards.tsx components/ai/genui-cards.test.tsx && \
git commit -m "feat(p3): gen UI card renderer mapping tool parts to P1 components"
```

---

### Task 5: gen UI 聊天面板（useChat + 渲染 text 与卡片 part）

**Files:**
- Create: `/Users/mac_1/msru.web/components/ai/genui-chat.tsx`
- Test: `/Users/mac_1/msru.web/components/ai/genui-chat.test.tsx`

**Interfaces:**
- Consumes: `@ai-sdk/react` `useChat` + `DefaultChatTransport`（api `/api/genui/chat`）；`GenUiToolPart`（Task 4）；`Markdown`（现有 `components/ai/markdown.tsx`）。
- Produces: `function GenUiChat(): JSX.Element` — 输入框 + 消息列表；文字 part 用 Markdown，tool part 用 GenUiToolPart。
- 同时导出纯函数 `function isToolPart(type: string): boolean`（供单测，判断 part 是否 gen UI 工具部件）。

- [ ] **Step 1: 写失败测试（isToolPart 纯函数）**

写 `/Users/mac_1/msru.web/components/ai/genui-chat.test.tsx`：
```tsx
import { expect, test } from "vitest";
import { isToolPart } from "./genui-chat";

test("recognizes gen UI tool parts", () => {
  expect(isToolPart("tool-provideProductComparison")).toBe(true);
  expect(isToolPart("tool-providePricing")).toBe(true);
  expect(isToolPart("tool-provideSolution")).toBe(true);
});

test("rejects non-tool parts", () => {
  expect(isToolPart("text")).toBe(false);
  expect(isToolPart("tool-unknown")).toBe(false);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-chat 2>&1 | tail -8`
Expected: FAIL。

- [ ] **Step 3: 实现 genui-chat.tsx**

写 `/Users/mac_1/msru.web/components/ai/genui-chat.tsx`：
```tsx
"use client";
import { useChat } from "@ai-sdk/react";
import { DefaultChatTransport } from "ai";
import { useState } from "react";
import { Markdown } from "./markdown";
import { GenUiToolPart } from "./genui-cards";
import { GENUI_TOOL_NAMES } from "@/lib/ai/genui-schema";

export function isToolPart(type: string): boolean {
  return GENUI_TOOL_NAMES.some((name) => type === `tool-${name}`);
}

export function GenUiChat() {
  const [input, setInput] = useState("");
  const { messages, sendMessage, status } = useChat({
    transport: new DefaultChatTransport({ api: "/api/genui/chat" }),
  });

  return (
    <div className="flex flex-col gap-4">
      <div className="flex flex-col gap-6">
        {messages.map((message) => (
          <div key={message.id}>
            <p className="mb-1 text-sm font-medium text-fd-muted-foreground">
              {message.role === "user" ? "你" : "MSRU 助手"}
            </p>
            {message.parts.map((part, i) => {
              if (part.type === "text") {
                return (
                  <div key={`${message.id}-t-${i}`} className="prose text-sm">
                    <Markdown text={part.text} />
                  </div>
                );
              }
              if (isToolPart(part.type) && "output" in part && part.output) {
                return <GenUiToolPart key={`${message.id}-c-${i}`} type={part.type} data={part.output} />;
              }
              return null;
            })}
          </div>
        ))}
      </div>
      <form
        onSubmit={(e) => {
          e.preventDefault();
          if (!input.trim()) return;
          void sendMessage({ text: input });
          setInput("");
        }}
        className="flex gap-2"
      >
        <input
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="问问 MSRU（如：对比 MES 和 WMS）"
          disabled={status === "streaming"}
          className="flex-1 rounded-full border border-border px-4 py-2 text-sm"
        />
        <button type="submit" disabled={status === "streaming"} className="rounded-full bg-primary px-4 py-2 text-sm text-primary-foreground">
          发送
        </button>
      </form>
    </div>
  );
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/mac_1/msru.web && pnpm test genui-chat 2>&1 | tail -8`
Expected: PASS（2 tests）。

- [ ] **Step 5: Commit**

```bash
cd /Users/mac_1/msru.web && git add components/ai/genui-chat.tsx components/ai/genui-chat.test.tsx && \
git commit -m "feat(p3): gen UI chat panel rendering text + card tool parts"
```

---

### Task 6: 挂载 gen UI 到营销站 + 环境变量模板 + 端到端验收

**Files:**
- Create: `/Users/mac_1/msru.web/app/(home)/ai/genui/page.tsx`（演示/入口页）
- Modify: `/Users/mac_1/msru.web/.env.example`

**Interfaces:**
- Consumes: `GenUiChat`（Task 5）。
- Produces: 可访问的 gen UI 页面 `/ai/genui`；完整环境变量模板；端到端验收。

- [ ] **Step 1: 写入口页**

写 `/Users/mac_1/msru.web/app/(home)/ai/genui/page.tsx`：
```tsx
import { GenUiChat } from "@/components/ai/genui-chat";
import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata({
  title: "MSRU 智能助手",
  description: "用自然语言探索 MSRU 产品、方案与定价。",
});

export default function GenUiPage() {
  return (
    <main className="max-w-3xl mx-auto px-6 py-16">
      <h1 className="text-4xl font-bold tracking-tight mb-8">MSRU 智能助手</h1>
      <GenUiChat />
    </main>
  );
}
```

- [ ] **Step 2: .env.example 增 DashScope 变量**

在 `/Users/mac_1/msru.web/.env.example` 追加：
```dotenv

# Gen UI 模型（DashScope OpenAI 兼容模式）
DASHSCOPE_API_KEY=your-dashscope-key
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
GENUI_MODEL=qwen-plus
```

- [ ] **Step 3: 构建验证**

Run: `cd /Users/mac_1/msru.web && pnpm build 2>&1 | tail -20`
Expected: 构建成功，`/ai/genui` 路由存在。

- [ ] **Step 4: 端到端验收（起服 + 浏览器交互）**

Run:
```bash
cd /Users/mac_1/msru.web && (pnpm dev &) && sleep 12 && \
curl -sS -o /dev/null -w "%{http_code} genui-page\n" http://localhost:3007/ai/genui
```
Expected: `200 genui-page`。然后用浏览器打开 `http://localhost:3007/ai/genui`，输入"对比 MES 和 WMS"，确认：① 流式文字出现；② 出现产品对比卡片（BentoCard 样式）。输入"你们怎么收费"确认出现定价卡。停 dev。

- [ ] **Step 5: 全量测试 + Commit**

Run:
```bash
cd /Users/mac_1/msru.web && pnpm test 2>&1 | tail -12
```
Expected: 全部 P1+P2+P3 单测 PASS。

```bash
cd /Users/mac_1/msru.web && git add "app/(home)/ai/genui" .env.example && \
git commit -m "feat(p3): mount gen UI panel at /ai/genui + env template + e2e"
```

---

## Self-Review

**Spec coverage（对照 spec §6 P3）：** 改用 AI SDK streamText + tools + qwen ✅（Task 2 provider + Task 3 route）；模型自主决定卡片 ✅（Task 3 tools 交模型编排）；卡片复用 P1 组件 ✅（Task 4 用 BentoCard/BentoGrid）；tool 输出 schema=卡片 props 契约 ✅（Task 1 共享 zod）；沿用 UI Message Stream SSE ✅（Task 3 toUIMessageStreamResponse）；保留 Dify 通道不动 ✅（新路由 `/api/genui/chat`，未改 `/api/chat`）；RAG 衔接=`execute` 取真库数据（Task 2），spec 所述"检索作为 tool / qwen 编排"落为 provideProductComparison 从 getProducts 取真实数据。

**Placeholder scan:** 无 TBD/TODO。需真实 API key / 浏览器交互的步骤用集成/e2e 验证并给出确切命令与预期；可纯测的（schema/tools.execute/isToolPart/卡片渲染）均 TDD。

**Type consistency:** `ProductComparisonInput`/`PricingInput`/`SolutionInput` 从 Task 1 genui-schema 定义，Task 2 tools inputSchema、Task 4 卡片 data 断言、Task 5 均引用同一处；`GENUI_TOOL_NAMES` 在 schema 定义，Task 5 `isToolPart` 复用；tool 名 `provideProductComparison`/`providePricing`/`provideSolution` 在 schema、tools、卡片 type（`tool-<name>`）、测试间逐字一致；`genuiModel`/`genuiTools` 命名在 provider/tools/route 间一致；卡片复用的 `BentoCard`/`BentoGrid`/`AccentColor` 均来自 P1 barrel `@/components/marketing`。

**跨计划依赖一致性：** P3 依赖 P2 `getProducts`（`ProductRecord` 字段 slug/title/tagline/accentColor 与 Task 2 execute 使用一致）与 P1 `BentoCard`（props icon/accentColor/title/tagline/description 与 Task 4 传参一致）。
