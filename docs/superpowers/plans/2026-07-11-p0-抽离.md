# P0 — msru.web 抽离实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `apps/docs`（Next.js 16 + Fumadocs + AI SDK）从 msru.platform monorepo 干净抽离为独立、可独立 `pnpm dev`/`build` 的项目 `msru.web`，切断 `@msru/ui` 等 workspace 依赖。

**Architecture:** 复制源码与配置到 `/Users/mac_1/msru.web`，排除构建产物；把仅用到的 `@msru/ui` 导出（标准 shadcn 组件 + 自定义 StatusPage + cn）内联进本地 `components/ui/` 与 `lib/`；改写 tsconfig paths、postcss、global.css 中所有指向 monorepo 的相对/别名引用为本地引用；补齐 package.json 依赖，独立 git 仓。

**Tech Stack:** Next.js 16.1.6, Fumadocs 16.4.9 (core/ui) + fumadocs-mdx 14.2.6, AI SDK (ai@6.0.101, @ai-sdk/react@3.0.103), Tailwind CSS 4, shadcn, Biome 2, pnpm, TypeScript 5.9.

## Global Constraints

- 包管理器：pnpm（独立项目，不用 monorepo workspace）。
- 目标目录：`/Users/mac_1/msru.web`（已有 git 仓，spec 已提交在 `docs/superpowers/specs/`）。
- 源目录（只读参考，勿改）：`/Users/mac_1/代码库/msru.platform/msru.platform/apps/docs`。
- monorepo 根（只读参考）：`/Users/mac_1/代码库/msru.platform/msru.platform`。
- Node 版本：与源一致（Next 16 要求 Node ≥ 20）。
- dev 端口：3007（`next dev -H :: --port 3007`），保持与源一致。
- 文件安全：不得删除源 monorepo 任何文件；本计划所有写操作只发生在 `/Users/mac_1/msru.web`。
- 禁止把明文密钥提交进 git：`.env` 必须进 `.gitignore`，只提交 `.env.example`（占位值）。

---

### Task 1: 复制源码与静态资源（排除构建产物）

**Files:**
- Create: `/Users/mac_1/msru.web/{app,components,content,lib,public}`（整目录复制）
- Create: `/Users/mac_1/msru.web/{mdx-components.tsx,source.config.ts,next.config.mjs,postcss.config.mjs,tsconfig.json,package.json}`
- Create: `/Users/mac_1/msru.web/.gitignore`

**Interfaces:**
- Consumes: 源 docs 目录全部源码。
- Produces: `msru.web` 下与源等价的源码树（尚未可编译，依赖后续任务修引用）。

- [ ] **Step 1: 复制源码目录与配置文件，显式排除构建产物**

```bash
SRC="/Users/mac_1/代码库/msru.platform/msru.platform/apps/docs"
DST="/Users/mac_1/msru.web"
# 用 rsync 排除构建产物与本地垃圾
rsync -a \
  --exclude 'node_modules' \
  --exclude '.next' \
  --exclude '.turbo' \
  --exclude '.source' \
  --exclude 'tsconfig.tsbuildinfo' \
  --exclude '.DS_Store' \
  --exclude 'source.config.d.ts' \
  --exclude 'source.config.d.ts.map' \
  "$SRC/app" "$SRC/components" "$SRC/content" "$SRC/lib" "$SRC/public" \
  "$SRC/mdx-components.tsx" "$SRC/source.config.ts" "$SRC/next.config.mjs" \
  "$SRC/postcss.config.mjs" "$SRC/tsconfig.json" "$SRC/package.json" "$SRC/README.md" \
  "$DST/"
```

- [ ] **Step 2: 写 `.gitignore`**

写 `/Users/mac_1/msru.web/.gitignore`：

```gitignore
node_modules
.next
.turbo
.source
*.tsbuildinfo
.DS_Store
.env
.env.local
next-env.d.ts
```

- [ ] **Step 3: 验证复制结果与排除生效**

Run:
```bash
cd /Users/mac_1/msru.web && ls app components content lib public && \
test ! -e node_modules && test ! -e .next && test ! -e .source && echo "EXCLUDE_OK"
```
Expected: 列出五个目录，末行输出 `EXCLUDE_OK`。

- [ ] **Step 4: Commit**

```bash
cd /Users/mac_1/msru.web && git add -A && \
git commit -m "feat(p0): copy docs source tree into standalone msru.web (exclude build artifacts)"
```

---

### Task 2: 内联 `cn` 工具，消除对 `@msru/ui/lib/utils` 的依赖

**Files:**
- Verify exists: `/Users/mac_1/msru.web/lib/cn.ts`（源已存在：`export { twMerge as cn } from "tailwind-merge";`）
- Modify: `/Users/mac_1/msru.web/components/pricing-table.tsx:4`

**Interfaces:**
- Consumes: `tailwind-merge`（已在 package.json）。
- Produces: 本地 `cn`，import 路径 `@/lib/cn`（`@/*` → `./*`，见 tsconfig）。

- [ ] **Step 1: 确认本地 cn 实现存在**

Run:
```bash
cat /Users/mac_1/msru.web/lib/cn.ts
```
Expected: `export { twMerge as cn } from "tailwind-merge";`

- [ ] **Step 2: 改 pricing-table.tsx 的 cn 引用**

`/Users/mac_1/msru.web/components/pricing-table.tsx` 第 4 行，由：
```tsx
import { cn } from "@msru/ui/lib/utils";
```
改为：
```tsx
import { cn } from "@/lib/cn";
```

- [ ] **Step 3: 验证无残留 `@msru/ui/lib/utils` 引用**

Run:
```bash
cd /Users/mac_1/msru.web && grep -rn "@msru/ui/lib" app components lib || echo "NO_MATCH"
```
Expected: `NO_MATCH`

- [ ] **Step 4: Commit**

```bash
cd /Users/mac_1/msru.web && git add components/pricing-table.tsx && \
git commit -m "refactor(p0): use local cn util instead of @msru/ui/lib/utils"
```

---

### Task 3: 迁入自定义 StatusPage 组件

**Files:**
- Create: `/Users/mac_1/msru.web/components/primitives/status-page.tsx`
- Create: `/Users/mac_1/msru.web/components/primitives/index.ts`
- Modify: `/Users/mac_1/msru.web/app/not-found.tsx:1`
- Modify: `/Users/mac_1/msru.web/app/error.tsx:3`

**Interfaces:**
- Consumes: `lucide-react`（已在 package.json）。
- Produces: `StatusPage`（default + named export），import 路径 `@/components/primitives`。

- [ ] **Step 1: 复制 StatusPage 源码到本地**

```bash
SRC="/Users/mac_1/代码库/msru.platform/msru.platform/packages/ui/src/components/primitives/status-page.tsx"
mkdir -p /Users/mac_1/msru.web/components/primitives
cp "$SRC" /Users/mac_1/msru.web/components/primitives/status-page.tsx
```

- [ ] **Step 2: 写 primitives barrel 只导出 StatusPage**

写 `/Users/mac_1/msru.web/components/primitives/index.ts`：

```ts
export { StatusPage } from "./status-page";
```

- [ ] **Step 3: 确认 StatusPage 内部无 @msru 依赖**

Run:
```bash
grep -n "@msru" /Users/mac_1/msru.web/components/primitives/status-page.tsx || echo "CLEAN"
```
Expected: `CLEAN`（该组件只 import lucide-react 与 React type）。

- [ ] **Step 4: 改 not-found.tsx 与 error.tsx 的 import**

`/Users/mac_1/msru.web/app/not-found.tsx` 第 1 行、`/Users/mac_1/msru.web/app/error.tsx` 第 3 行，将：
```tsx
import { StatusPage } from "@msru/ui/components/primitives";
```
均改为：
```tsx
import { StatusPage } from "@/components/primitives";
```

- [ ] **Step 5: 验证**

Run:
```bash
cd /Users/mac_1/msru.web && grep -rn "@msru/ui/components/primitives" app || echo "NO_MATCH"
```
Expected: `NO_MATCH`

- [ ] **Step 6: Commit**

```bash
cd /Users/mac_1/msru.web && git add components/primitives app/not-found.tsx app/error.tsx && \
git commit -m "feat(p0): vendor StatusPage primitive locally"
```

---

### Task 4: 用 shadcn 生成本地 UI 组件（accordion/breadcrumb/button/card/label/select/slider/switch）

**Files:**
- Create: `/Users/mac_1/msru.web/components/ui/{accordion,breadcrumb,button,card,label,select,slider,switch}.tsx`
- Create/Modify: `/Users/mac_1/msru.web/components.json`（shadcn 配置）
- Create: `/Users/mac_1/msru.web/lib/utils.ts`（shadcn 默认 cn，若 shadcn 生成需要）

**Interfaces:**
- Consumes: shadcn CLI（devDep 已有 `shadcn: ^3.8.5`）、radix-ui、class-variance-authority。
- Produces: 本地组件 `Button`、`buttonVariants`、`Switch`、`Accordion*`、`Breadcrumb*`、`Card`、`CardContent`、`Label`、`Select*`、`Slider`，统一 import 路径 `@/components/ui/<name>`。

**背景说明（给实现者）：** 源项目从 `@msru/ui/components`（barrel）导入这些。独立项目改为逐组件从 `@/components/ui/<name>` 导入。这些都是标准 shadcn 组件，用 shadcn CLI 生成即可，避免手写。生成后需核对 API 与源用法一致（尤其 `buttonVariants` 具名导出、`CardContent`）。

- [ ] **Step 1: 初始化 shadcn（若无 components.json）**

Run（在 `/Users/mac_1/msru.web`）：
```bash
cd /Users/mac_1/msru.web && pnpm dlx shadcn@3 init --base-color neutral --css app/global.css --yes 2>&1 | tail -5
```
Expected: 生成 `components.json` 与 `lib/utils.ts`。若交互卡住，手写 `components.json`（见 Step 2 兜底）。

- [ ] **Step 2:（兜底）手写 components.json**

若 Step 1 失败，写 `/Users/mac_1/msru.web/components.json`：

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "app/global.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/cn",
    "ui": "@/components/ui"
  }
}
```

- [ ] **Step 3: 生成需要的组件**

Run:
```bash
cd /Users/mac_1/msru.web && \
pnpm dlx shadcn@3 add accordion breadcrumb button card label select slider switch --yes --overwrite 2>&1 | tail -10
```
Expected: `components/ui/` 下生成 8 个 `.tsx`。

- [ ] **Step 4: 核对导出符合源用法**

Run:
```bash
cd /Users/mac_1/msru.web && \
grep -l "buttonVariants" components/ui/button.tsx && \
grep -l "CardContent" components/ui/card.tsx && \
grep -l "BreadcrumbSeparator" components/ui/breadcrumb.tsx && \
grep -l "AccordionTrigger" components/ui/accordion.tsx && echo "EXPORTS_OK"
```
Expected: `EXPORTS_OK`。若某导出缺失（如 shadcn 版本改了 API），补齐对应 export。

- [ ] **Step 5: 统一 shadcn 生成组件内部的 cn import 到 `@/lib/cn`**

shadcn 默认从 `@/lib/utils` import cn。本项目 cn 在 `@/lib/cn`。改：
```bash
cd /Users/mac_1/msru.web && \
sed -i '' 's#@/lib/utils#@/lib/cn#g' components/ui/*.tsx && \
grep -rn "@/lib/utils" components/ui || echo "CN_UNIFIED"
```
Expected: `CN_UNIFIED`（若 shadcn 生成了 `lib/utils.ts`，保留或删除均可，但组件统一引用 `@/lib/cn`）。

- [ ] **Step 6: Commit**

```bash
cd /Users/mac_1/msru.web && git add components/ui components.json lib && \
git commit -m "feat(p0): generate local shadcn ui components (accordion/breadcrumb/button/card/label/select/slider/switch)"
```

---

### Task 5: 改写所有 `@msru/ui/components*` import 为本地路径

**Files:**
- Modify: `/Users/mac_1/msru.web/components/apple-footer.tsx:3-11`
- Modify: `/Users/mac_1/msru.web/components/ai/search.tsx:3`
- Modify: `/Users/mac_1/msru.web/app/docs/layout.tsx:1`
- Modify: `/Users/mac_1/msru.web/components/pricing-table.tsx:3`
- Modify: `/Users/mac_1/msru.web/app/(home)/company/brand/fractal/page.tsx:3-16`

**Interfaces:**
- Consumes: Task 4 生成的 `@/components/ui/*`。
- Produces: 全项目零 `@msru/ui` 引用。

**映射规则（给实现者）：** `@msru/ui/components/ui/accordion` → `@/components/ui/accordion`；`@msru/ui/components/ui/breadcrumb` → `@/components/ui/breadcrumb`；`@msru/ui/components`（barrel，含 Button/buttonVariants/Switch/Card/CardContent/Label/Select*/Slider）→ 按实际用到的符号拆分到对应 `@/components/ui/<name>`。

- [ ] **Step 1: apple-footer.tsx（accordion + breadcrumb）**

`/Users/mac_1/msru.web/components/apple-footer.tsx` 顶部：
```tsx
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@msru/ui/components/ui/accordion";
import { Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from "@msru/ui/components/ui/breadcrumb";
```
改为：
```tsx
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
import { Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from "@/components/ui/breadcrumb";
```

- [ ] **Step 2: search.tsx 与 docs/layout.tsx（buttonVariants）**

两文件里：
```tsx
import { buttonVariants } from "@msru/ui/components";
```
改为：
```tsx
import { buttonVariants } from "@/components/ui/button";
```

- [ ] **Step 3: pricing-table.tsx（Button, Switch）**

`/Users/mac_1/msru.web/components/pricing-table.tsx` 第 3 行：
```tsx
import { Button, Switch } from "@msru/ui/components";
```
改为：
```tsx
import { Button } from "@/components/ui/button";
import { Switch } from "@/components/ui/switch";
```

- [ ] **Step 4: fractal/page.tsx（Button/Card/CardContent/Label/Select*/Slider/Switch）**

`/Users/mac_1/msru.web/app/(home)/company/brand/fractal/page.tsx` 第 3-16 行的：
```tsx
import { Button, Card, CardContent, Label, Select, SelectContent, SelectGroup, SelectItem, SelectTrigger, SelectValue, Slider, Switch } from "@msru/ui/components";
```
改为：
```tsx
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectGroup, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Slider } from "@/components/ui/slider";
import { Switch } from "@/components/ui/switch";
```

- [ ] **Step 5: 验证全项目零 @msru 引用**

Run:
```bash
cd /Users/mac_1/msru.web && grep -rn "@msru/" app components lib --include='*.ts' --include='*.tsx' || echo "NO_MSRU_REFS"
```
Expected: `NO_MSRU_REFS`

- [ ] **Step 6: Commit**

```bash
cd /Users/mac_1/msru.web && git add -A && \
git commit -m "refactor(p0): replace all @msru/ui imports with local components"
```

---

### Task 6: 修 tsconfig / postcss / global.css 中指向 monorepo 的引用

**Files:**
- Modify: `/Users/mac_1/msru.web/tsconfig.json`
- Modify: `/Users/mac_1/msru.web/postcss.config.mjs`
- Modify: `/Users/mac_1/msru.web/app/global.css`

**Interfaces:**
- Produces: 独立可解析的 TS 路径别名与 Tailwind/PostCSS 配置，不指向 monorepo。

- [ ] **Step 1: tsconfig.json 移除 @msru/ui path 映射**

`/Users/mac_1/msru.web/tsconfig.json` 的 `compilerOptions.paths`，删除 `@msru/ui/*` 一行，保留其余：
```json
"paths": {
  "@/*": ["./*"],
  "fumadocs-mdx:collections/*": [".source/*"]
}
```

- [ ] **Step 2: postcss.config.mjs 改为直接用 @tailwindcss/postcss**

`/Users/mac_1/msru.web/postcss.config.mjs` 由：
```js
export { default } from "@msru/ui/postcss.config";
```
整体替换为：
```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

- [ ] **Step 3: global.css 修 `@source` 相对路径**

`/Users/mac_1/msru.web/app/global.css` 中：
```css
@source "../../../packages/ui/src";
```
本项目已把用到的组件搬进 `components/`，删除该行（Tailwind 会默认扫描项目内源码）。其余 `@import`（tailwindcss / fumadocs-ui/css / katex / shadcn/tailwind.css）与颜色 token 全部保留不动。

- [ ] **Step 4: 验证无 monorepo 相对路径与别名残留**

Run:
```bash
cd /Users/mac_1/msru.web && \
grep -rn "packages/ui" app tsconfig.json postcss.config.mjs || echo "NO_MONOREPO_PATHS" && \
grep -n "@msru/ui" tsconfig.json || echo "NO_ALIAS"
```
Expected: 两行分别 `NO_MONOREPO_PATHS`、`NO_ALIAS`。

- [ ] **Step 5: Commit**

```bash
cd /Users/mac_1/msru.web && git add tsconfig.json postcss.config.mjs app/global.css && \
git commit -m "refactor(p0): decouple tsconfig/postcss/global.css from monorepo paths"
```

---

### Task 7: 重写 package.json（独立化：去 workspace 依赖、补齐直接依赖、加 biome）

**Files:**
- Modify: `/Users/mac_1/msru.web/package.json`
- Create: `/Users/mac_1/msru.web/biome.json`

**Interfaces:**
- Produces: 可 `pnpm install` 的独立依赖清单；本地 biome 配置（等价 monorepo base）。

**背景（给实现者）：** 源 package.json 有 `"@msru/ui": "workspace:*"` 需删。docs 自身直接 import 但需确认在列的依赖：`class-variance-authority`（shadcn 组件底层）、`radix-ui`（shadcn 底层）、`clsx`（保留）。`@ai-sdk/openai-compatible` 源码未用但 P3 会用到，保留。biome 源项目靠向上查找 monorepo 根，独立后需自带。

- [ ] **Step 1: 删除 workspace 依赖，加 next-themes（StatusPage/主题需要时）与脚本**

编辑 `/Users/mac_1/msru.web/package.json`：
- 删除 `dependencies` 中 `"@msru/ui": "workspace:*"`。
- 确认保留 `class-variance-authority`、`radix-ui`、`clsx`、`tailwind-merge`、`tailwindcss-animate`（shadcn 组件运行所需）。
- `name` 从 `"docs"` 改为 `"msru-web"`。
- scripts 保持：`dev`/`build`/`start`/`check-types`/`postinstall: fumadocs-mdx`/`lint: biome check`/`format`/`lint:fix` 不变。

- [ ] **Step 2: 写本地 biome.json（等价 monorepo base）**

写 `/Users/mac_1/msru.web/biome.json`（内容取自 `packages/biome-config/base.json`，路径 include 改本地）：

```json
{
  "$schema": "https://biomejs.dev/schemas/2.3.12/schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  "formatter": { "enabled": true, "indentStyle": "space", "indentWidth": 2, "lineEnding": "lf", "lineWidth": 120 },
  "javascript": { "formatter": { "quoteStyle": "double" } },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "style": { "noNonNullAssertion": "warn" },
      "suspicious": { "noExplicitAny": "warn", "noAssignInExpressions": "warn", "noArrayIndexKey": "warn" }
    }
  },
  "css": { "parser": { "cssModules": true }, "formatter": { "enabled": true } },
  "files": { "includes": ["**", "!app/global.css", "!.next/**", "!.source/**", "!node_modules/**"] }
}
```

- [ ] **Step 3: 安装依赖**

Run:
```bash
cd /Users/mac_1/msru.web && pnpm install 2>&1 | tail -15
```
Expected: 安装成功，无 `workspace:` 协议解析错误。

- [ ] **Step 4: 验证无 workspace 残留**

Run:
```bash
cd /Users/mac_1/msru.web && grep -n "workspace:" package.json || echo "NO_WORKSPACE"
```
Expected: `NO_WORKSPACE`

- [ ] **Step 5: Commit**

```bash
cd /Users/mac_1/msru.web && git add package.json biome.json pnpm-lock.yaml && \
git commit -m "build(p0): standalone package.json + local biome config, drop workspace deps"
```

---

### Task 8: 处理环境变量（去明文密钥，建 .env.example）

**Files:**
- Create: `/Users/mac_1/msru.web/.env.example`
- Verify: `/Users/mac_1/msru.web/.env`（保留本地，已被 .gitignore 忽略）

**Interfaces:**
- Produces: 不含真实密钥的 `.env.example` 模板；真实 `.env` 不进 git。

**安全事实（给实现者）：** 源 `.env` 含明文 `DIFY_API_KEY="app-lAMH93hbSklDLhyjswhLwE8y"`。Task 1 已复制它，但 Task 1 的 .gitignore 已忽略 .env，故不会进 git。此任务补一份占位模板。

- [ ] **Step 1: 写 .env.example（占位值，无真实密钥）**

写 `/Users/mac_1/msru.web/.env.example`：

```dotenv
# GitHub 分支（文档 "编辑此页" 链接用）
GITHUB_BRANCH=master

# Dify 云服务（站内 AI 问答后端）
DIFY_API_KEY=your-dify-app-key-here
DIFY_BASE_URL=https://api.dify.ai/v1
```

- [ ] **Step 2: 确认 .env 未被 git 跟踪**

Run:
```bash
cd /Users/mac_1/msru.web && git check-ignore .env && echo "ENV_IGNORED"
```
Expected: 输出 `.env` 与 `ENV_IGNORED`。

- [ ] **Step 3: Commit**

```bash
cd /Users/mac_1/msru.web && git add .env.example && \
git commit -m "chore(p0): add .env.example template (no secrets committed)"
```

---

### Task 9: 独立跑通 dev 与 build（验收）

**Files:**
- 无新增；验证整体可运行。

**Interfaces:**
- Consumes: 前 8 个 task 的全部产物。
- Produces: 可独立运行的 `msru.web`（P0 完成判据）。

- [ ] **Step 1: 生成 fumadocs 内容与类型（postinstall 已跑，手动补一次）**

Run:
```bash
cd /Users/mac_1/msru.web && pnpm fumadocs-mdx && pnpm next typegen 2>&1 | tail -10
```
Expected: 生成 `.source/` 目录，无报错。

- [ ] **Step 2: 类型检查**

Run:
```bash
cd /Users/mac_1/msru.web && pnpm check-types 2>&1 | tail -20
```
Expected: 通过，或仅剩与抽离无关的既有告警（`next.config.mjs` 已设 `typescript.ignoreBuildErrors`，但此步单独验证 tsc）。若报 `Cannot find module '@msru/ui...'`，说明有遗漏引用，回到 Task 5 补。

- [ ] **Step 3: 生产构建**

Run:
```bash
cd /Users/mac_1/msru.web && pnpm build 2>&1 | tail -30
```
Expected: `Compiled successfully`，生成 `.next/`。无 `Module not found: @msru/ui` 类错误。

- [ ] **Step 4: 启动 dev 并抽查首页**

Run:
```bash
cd /Users/mac_1/msru.web && (pnpm dev &) && sleep 12 && \
curl -sS -o /dev/null -w "%{http_code}" http://localhost:3007/ && echo " HOME_OK" && \
curl -sS -o /dev/null -w "%{http_code}" http://localhost:3007/docs && echo " DOCS_OK"
```
Expected: 两行分别 `200 HOME_OK`、`200 DOCS_OK`（或 docs 首页对应状态码）。测完手动停掉 dev 进程。

- [ ] **Step 5: 最终验收提交**

```bash
cd /Users/mac_1/msru.web && git add -A && \
git commit -m "chore(p0): standalone msru.web builds and runs independently" --allow-empty
```

---

## Self-Review

**Spec coverage（对照 spec §3 P0 抽离）：**
- pnpm ✅ Task 7 / git 独立仓 ✅（已存在，各 task commit）/ 只提取用到的 @msru/ui ✅ Task 2-5。
- 排除构建产物 ✅ Task 1 / 依赖内联 ✅ Task 7 / tsconfig path 修正 ✅ Task 6 / tailwind·biome 独立 ✅ Task 6-7。
- 验收「pnpm install && dev && build 独立通过」✅ Task 9。
- 额外覆盖 spec 未显式列出但侦察发现的必需项：global.css `@source` 修正（Task 6）、postcss 传递依赖切断（Task 6）、.env 明文密钥处理（Task 8）。

**Placeholder scan:** 无 TBD/TODO；所有步骤含精确路径、命令、预期输出与完整代码。

**Type consistency:** cn 统一 `@/lib/cn`；UI 组件统一 `@/components/ui/<name>`；StatusPage 统一 `@/components/primitives`。Task 4 Step 4 显式核对 `buttonVariants`/`CardContent`/`BreadcrumbSeparator`/`AccordionTrigger` 具名导出与源用法一致。
